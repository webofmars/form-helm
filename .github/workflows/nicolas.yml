name: nicolas

# Trigger the workflow manually or on every push
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  list-files:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: List files in nicolas/helm/myapp
      run: |
        echo "📁 Listing files in nicolas/helm/myapp directory:"
        echo "================================================"

        if [ -d "nicolas/helm/myapp" ]; then
          echo "✅ Directory exists. Contents:"

        else
          echo "❌ Directory 'nicolas/helm/myapp' does not exist"
          echo "📂 Available directories at root level:"
          ls -la ./
        fi
